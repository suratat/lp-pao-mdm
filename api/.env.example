# ค่าจริงของ staging/production ต้องมาจาก Docker secrets / env ของเครื่องจริงเท่านั้น (ห้าม commit ค่าจริง)
DATABASE_URL=postgres://mdm_app_user:CHANGE_ME@localhost:5432/mdm

JWT_JWKS_URI=https://iam.lp-pao.go.th/realms/lp-pao/protocol/openid-connect/certs
JWT_ISSUER=https://iam.lp-pao.go.th/realms/lp-pao
JWT_AUDIENCE=mdm-api

VAULT_ADDR=http://vault.internal:8200
VAULT_TOKEN=CHANGE_ME
