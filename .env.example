# สำหรับ dev/test migrations เท่านั้น (ใช้กับ db/docker-compose.yml)
# ค่าจริงของ staging/production ต้องมาจาก Docker secrets / env ของเครื่องจริงเท่านั้น (ห้าม commit ค่าจริง)
DATABASE_URL=postgres://mdm_migrator:mdm_migrator_dev_only@localhost:55432/mdm_test
